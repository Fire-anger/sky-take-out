<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <!-- 新增套餐并返回套餐的主键id -->
    <insert id="insert" parameterType="setmeal" keyProperty="id" useGeneratedKeys="true">
        insert into setmeal (category_id,name,price,status,description,image,create_time,update_time,create_user,update_user)
        values
        (#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>


    <!-- 套餐分页查询 -->
    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select s.*,c.name categoryName from
            setmeal s
        left join
            category c
        on
            s.category_id = c.id
        <where>
            <if test="name != null"> and name like concat('%',#{name},'%')</if>
            <if test="status != null"> and status = #{status}</if>
            <if test="categoryId != null"> and cateory_id = #{categoryId}</if>
        </where>
        order by create_time desc
    </select>

    <!-- 修改套餐 -->
    <update id="update">
        update setmeal
        <set>
            <if test="categoryId">category_id = #{categoryId},</if>
            <if test="description">description = #{description},</if>
            <if test="image">image = #{image},</if>
            <if test="name">name = #{name},</if>
            <if test="price">price = #{price},</if>
            <if test="status">status = #{status},</if>
            <if test="updateTime">update_time = #{updateTime},</if>
            <if test="updateUser">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>
</mapper>